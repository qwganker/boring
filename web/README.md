# Boring Web (Vue + Element Plus)

Quick start for the frontend scaffold used for the `web/` folder.

Requirements
- Node.js (>=16 recommended)

Install and run

```bash
cd web
npm install
npm run dev
```

App will be available at `http://localhost:5173` by default.
